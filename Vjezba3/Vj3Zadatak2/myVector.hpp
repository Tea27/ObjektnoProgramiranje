#pragma once

#include <iostream>
#include <vector>

void vectorAndNumOfElements(std::vector<int>& vect, int n);

void vectorAndInterval(std::vector<int>& vect, int a, int b);

std::vector<int> isInFirstNotInSecond(std::vector<int> first, std::vector<int> second);

void printVector(std::vector<int> vect, int n);
